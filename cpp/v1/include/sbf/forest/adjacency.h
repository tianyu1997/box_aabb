// SafeBoxForest — Adjacency computation (vectorized O(N²D))
// Module: sbf::forest
// Renamed from deoverlap.h — the function computes adjacency, not deoverlap.
#pragma once

// Re-export from original location (content identical)
#include "sbf/forest/deoverlap.h"
