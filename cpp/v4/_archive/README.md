Archived generated build directories for safeboxforest/v4.

- `build_x64_legacy_20260402/` is a stale Visual Studio/CMake build tree kept only for reference.
- It contains removed historical targets such as `test_certified_verifier` and should not be used for current builds or tests.
- The active VS Code/CMake Tools build directory is the workspace-root `build_x64/`.
