在当前实验2  C. Link-Envelope Pipeline Comparison和实验3 D. End-to-End Baseline Comparison中间需要插入一个实验对比不同envelope管线在marcucci场景上的build表现。
包括 epiaabb:  IFK, crit Sample   link interval envelope: AABB sub=4,  AABB grid sub=4, resolution=0.04, hull16 grid resolution=0.04 共2*3 =6组对照。
记得预热各个envelope的LECT！ 可以用相同seed先跑一次（先删除缓存，完全无缓存cold），第二次再跑的话cache应该就是完全命中（warm）！把cold和warm模式的数据都记录下来。
